<!doctype html>
<html>
    <head>
        {% include "partials/head.njk" %}
    </head>
    <body>
        <div class="page">
            {% include "partials/nav.njk" %}
            <div class="content">
                <h1>{{ title | safe }}
                    <span style="font-size: 12px">{{ blogdate | safe }}</span>
                </h1>
                {{ content | safe }}
            </div>
        </div>
    </body>
</html>